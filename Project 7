#include<iostream>
#include<conio.h>

using namespace std;

int main()
{
	int n;

	cout << "Insert the number" << endl;
	cin >> n;

	if (n > 0)
		cout << (n / 2)*(2 + (n - 1)) << endl;
	else if (n == 0)
		return 0;
	else
		cout << "Wrong Input." << endl;

	_getch();
	return 0;
}
